package main
import "fmt"

func main(){
  //if initialization; condition
  if value, ok := bmi(1.50, 55) ; ok{
    fmt.printf("BMI: %f\n", value)
  }

  //value and ok are the variable that represent the bmi function call return type 
}

func bmi(height)
